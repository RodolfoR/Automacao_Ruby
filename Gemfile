# frozen_string_literal: true

source "https://rubygems.org" do #define onde o projeto vai “buscar” as bibliotecas para serem instaladas.
    gem 'cucumber' # serve escrever as funcionalidades usando Gherkin
    gem 'httparty'     # serve para criar as requests http
    gem 'rspec'         # serve para realizar as validações dos cenários esperados.
    gem 'faker'
end
